/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "", //keep this only on dev
    output: "export",
    assetPrefix: "./",
    reactStrictMode: false,
};

export default nextConfig;
